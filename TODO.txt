Add a stamp to last hit with total_hit ground truth RAM, and a (x, y) coordinate. 
This makes sure the ammo that we shoot isn't going to be more than 3, wasting ammo and ruining accurary + wasting time.
Needs rethinking: stairs scene in STAGE 2!!

Add memory or some kind of theta input: when to SURELY dodge when there is danger. Make it learnable by previous agents who took damage there consistently.
Folyomány: create a function that makes the agents dodge in these timestamps.

Retrain/fine-tune the YOLO detector (best.pt). The aim math is verified correct (blend_gain=1.0 full lock-on, drift disabled, guncon calib measured/correct), so the remaining accuracy ceiling is DETECTOR quality, not aiming.
Symptoms (2026-09-23 frame-dump diagnosis): extreme-edge enemies (cx<0.1) come back low-confidence (~0.26) and slightly imprecise; false positives on muzzle flashes / fire / clutter (e.g. a flame boxed as ENEMY at conf 0.45) pull aim off the real enemy in chaotic/damage frames.
Plan: label more frames (edge enemies + hard negatives like muzzle flashes/fire), then fine-tune per the offline workflow in detector.py's footer. Use run_eval.py --dump-frames to gather corpus.

Add dodge memory mechanics for DANGER and fixed red character 100% accurate hit moments!
So that the agents will learn exact moments when to shoot. They already know which screen they are at, so it should be easy to keep separated.
